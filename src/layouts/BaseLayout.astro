---
const { pageSubtitle, pageTrailingTitle, containerClass, header } = {
  pageSubtitle: "Portfolio",
  pageTrailingTitle: " - Christian van der Loo",
  containerClass: "max-w-screen-lg px-4 sm:px-12 mx-auto pb-24",
  header: true,
  ...Astro.props,
};

import "@fontsource/ia-writer-quattro/all.css";
import Header from "../components/Header.astro";
import "../base.scss";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageSubtitle + pageTrailingTitle}</title>
  </head>
  <body class="dark:bg-slate-900 dark:text-white">
    {
      header && (
        <div class="max-w-screen-lg mx-auto">
          <Header />
        </div>
      )
    }
    <div class={containerClass}>
      <slot />
    </div>
    <div class="h-24 flex items-center justify-center">
      <span class="italic text-gray-500"></span>
    </div>
  </body>
</html>
